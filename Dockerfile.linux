# escape=`

FROM lacledeslan/gamesvr-srcds-hl2dm:linux

LABEL maintainer "Laclede's LAN <contact@lacledeslan.com>"

ENV IMAGE="gamesvr-srcds-hl2dm-freeplay:linux"

HEALTHCHECK NONE

USER root

COPY ./dist /app/bin/

COPY ./dist.linux /app/bin/hl2mp/

# CLEAN UP
RUN usermod -l HL2DM.Freeplay HL2DM
	
WORKDIR /app/bin/

USER HL2DM.Freeplay
