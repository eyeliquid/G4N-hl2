# escape=`

FROM lacledeslan/gamesvr-hl2dm

LABEL maintainer "Laclede's LAN <contact@lacledeslan.com>"

ENV IMAGE="gamesvr-hl2dm-freeplay"

HEALTHCHECK NONE

COPY ./dist /app/hl2mp/

COPY ./dist.linux /app/hl2mp/

# UPDATE USERNAME
RUN usermod -l HL2DMFreeplay HL2DM

USER HL2DMFreeplay

WORKDIR /app/
